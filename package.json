{
  "name": "hugo-vizjp",
  "version": "1.0.0",
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --minify",
    "preview": "hugo server --environment production"
  },
  "devDependencies": {
    "hugo-extended": "0.149.0"
  }
}